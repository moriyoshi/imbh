[package]
name = "imbh-otlp"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "OTLP decode → normalized imbh rows (prost message types only)."

[dependencies]
imbh-core.workspace = true
opentelemetry-proto.workspace = true
prost.workspace = true
# Optional self-observability facade (ARCHITECTURE.md §11), off by default.
tracing = { workspace = true, optional = true }

[features]
# Emit decode spans/events through the `tracing` facade (off by default — §11 footprint lever).
tracing = ["dep:tracing"]
